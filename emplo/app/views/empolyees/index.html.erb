<h1>Listing empolyees</h1>

<table>
  <tr>
    <th>Firstname</th>
    <th>Lastname</th>
    <th>Birthday</th>
    <th>Email</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @empolyees.each do |empolyee| %>
  <tr>
    <td><%= empolyee.firstname %></td>
    <td><%= empolyee.lastname %></td>
    <td><%= empolyee.birthday %></td>
    <td><%= empolyee.email %></td>
    <td><%= empolyee.comment %></td>
    <td><%= link_to 'Show', empolyee %></td>
    <td><%= link_to 'Edit', edit_empolyee_path(empolyee) %></td>
    <td><%= link_to 'Destroy', empolyee, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Empolyee', new_empolyee_path %>
